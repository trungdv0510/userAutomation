package readExcel;

public class dataMap {
	public enum test1 {No,Testname, search, Result, Error}
	public enum login{No,Testname,Username,Password, Alert, Result,Error}
	public enum regester{No, Testname,username, phoneNumber, address,password,repeatPassword, resultMessage,Result,Error}
}
